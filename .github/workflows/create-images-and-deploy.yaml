name: Create docker images and deploy
run-name: Create docker images and deploy
on: push
env:
  IP: ${{ vars.TUOTANTO_IP }}
  KAYTTAJA: ${{ vars.TUOTANTO_KAYTTAJA }}
  REPON_SIJAINTI: ${{ vars.TUOTANTO_KANSIO }}
jobs:
  create-images-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - run: ssh $KAYTTAJA@$IP:$REPON_SIJAINTI "git pull"
      - run: ssh $KAYTTAJA@$IP:$REPON_SIJAINTI "./rakenna.sh -b huttustutka-back -f huttustutka-front"
      - run: ssh $KAYTTAJA@$IP:$REPON_SIJAINTI "./aja.sh -b huttustutka-back -f huttustutka-front"
